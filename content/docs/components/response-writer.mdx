---
slug: components-response-writer
title: Response Writer
description: An auto scrolling text display that smoothly keeps the latest response always visible
---

<DemoCodePreview
    path="demo/components/response-writer/default.tsx"
    v0Link="/ai/response-writer"
/>

## Installation

<Tabs defaultValue="cli" className="mt-3">
    <TabsList>
        <TabsTrigger value="cli">CLI</TabsTrigger>
        <TabsTrigger value="manual">Manual</TabsTrigger>
    </TabsList>
    <TabsContent value="cli" className="mt-4">
        <InstallationCommand command={`npx shadcn@latest add @paceui-ui/response-writer`} />
    </TabsContent>
    <TabsContent value="manual" className="mt-4">
        <Steps>
            <Step>
                Install the following dependencies
            </Step>
            <InstallationCommand command="npm install gsap @gsap/react" />
            <Step>
                Copy and paste the following code into your project
            </Step>
            <CodePreview path='components/ai/response-writer.tsx' collapsible></CodePreview>

            <Step>
                Update the import paths to fit your project setup
            </Step>
        </Steps>
    </TabsContent>
</Tabs>

## Props

### ResponseWriter

| Prop | Type   | Default |
|------|--------|---------|
| text | string | -       |

