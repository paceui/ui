---
slug: components-swap
title: Swap
description: A flexible swap component that animates content changes with smooth customizable transition effects.
---

<DemoCodePreview
    path="demo/components/swap/default.tsx"
    v0Link="/gsap/swap"
/>

## Installation

<Tabs defaultValue="cli" className="mt-3">
    <TabsList>
        <TabsTrigger value="cli">CLI</TabsTrigger>
        <TabsTrigger value="manual">Manual</TabsTrigger>
    </TabsList>
    <TabsContent value="cli" className="mt-4">
        <InstallationCommand command={`npx shadcn@latest add @paceui-ui/swap`} />
    </TabsContent>
    <TabsContent value="manual" className="mt-4">
        <Steps>
            <Step>
                Install the following dependencies
            </Step>
            <InstallationCommand command="npm install gsap @gsap/react" />
            <Step>
                Copy and paste the following code into your project
            </Step>
            <CodePreview path='components/gsap/swap.tsx' collapsible></CodePreview>

            <Step>
                Update the import paths to fit your project setup
            </Step>
        </Steps>
    </TabsContent>
</Tabs>

## Examples

### Headline

<DemoCodePreview
    path="demo/components/swap/headline.tsx"
    v0Link="/gsap/swap"
/>

## Props

### Swap

| Prop     | Type     | Default |
|----------|----------|---------|
| state    | T        | -       |
| duration | number   | 0.4     |
| effects  | string[] | -       |
