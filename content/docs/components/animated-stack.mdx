---
slug: components-animated-stack
title: Animated Stack
description: A smooth stacked card component that expands on hover and collapses with animation.
---

<DemoCodePreview
    path="demo/components/animated-stack/default.tsx"
    v0Link="/gsap/animated-stack"
/>

## Installation

<Tabs defaultValue="cli" className="mt-3">
    <TabsList>
        <TabsTrigger value="cli">CLI</TabsTrigger>
        <TabsTrigger value="manual">Manual</TabsTrigger>
    </TabsList>
    <TabsContent value="cli" className="mt-4">
        <InstallationCommand command={`npx shadcn@latest add "@paceui-ui/animated-stack`} />
    </TabsContent>
    <TabsContent value="manual" className="mt-4">
        <Steps>
            <Step>
                Install the following dependencies
            </Step>
            <InstallationCommand command="npm install gsap @gsap/react" />
            <Step>
                Copy and paste the following code into your project
            </Step>
            <CodePreview path='components/gsap/animated-stack.tsx' collapsible></CodePreview>

            <Step>
                Update the import paths to fit your project setup
            </Step>
        </Steps>
    </TabsContent>
</Tabs>

## Props

### AnimatedStack

| Prop         | Type         | Default |
|--------------|--------------|---------|
| visibleCount | number       | 3       |
| gap          | number       | 8       |
| offset       | number       | 8       |
| direction    | `up`\|`down` | `up`    |
