---
title: MCP
description: Getting Started with the Registry MCP in PaceUI
---

Paste the following code into your IDE to configure the Registry MCP.

<Tabs defaultValue="cursor">
    <TabsList>
        <TabsTrigger value="cursor">Cursor</TabsTrigger>
        <TabsTrigger value="windsurf">Windsurf</TabsTrigger>
    </TabsList>
    <TabsContent value="cursor">
```json
// Copy and paste the code into .cursor/mcp.json
{
    "mcpServers": {
        "shadcn": {
            "command": "npx",
            "args": ["-y", "shadcn@canary", "registry:mcp"],
            "env": {
                "REGISTRY_URL": "https://ui.paceui.com/r/mcp.json"
            }
        }
    }
}
```
    </TabsContent>
    <TabsContent value="windsurf">
```json
// Copy and paste the code into codeium/windsurf/mcp_config.json
{
    "mcpServers": {
        "shadcn": {
            "command": "npx",
            "args": ["-y", "shadcn@canary", "registry:mcp"],
            "env": {
                "REGISTRY_URL": "https://ui.paceui.com/r/mcp.json"
            }
        }
    }
}
```
    </TabsContent>
</Tabs>
