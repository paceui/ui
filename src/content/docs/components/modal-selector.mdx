---
title: Modal Selector
description: AI-focused model selector with preview, tooltips, and capability indicators built-in.
---

<DemoCodePreview
    path="demo/components/modal-selector/default.tsx"
    v0Link="/gsap/modal-selector"
/>

## Installation

<Tabs defaultValue="cli">
    <TabsList>
        <TabsTrigger value="cli">CLI</TabsTrigger>
        <TabsTrigger value="manual">Manual</TabsTrigger>
    </TabsList>
    <TabsContent value="cli">
        ```npm
        npx shadcn@latest add @paceui-ui/modal-selector
        ```
    </TabsContent>
    <TabsContent value="manual">
        <Steps>
            <Step>
                **Install the following dependencies**
            </Step>
            ```npm
            npm install gsap @gsap/react
            ```
            <Step>
                **Copy and paste the following code into your project**
            </Step>
            <CodePreview path='components/ai/modal-selector.tsx' collapsible></CodePreview>

            <Step>
                **Update the import paths to fit your project setup**
            </Step>

        </Steps>
    </TabsContent>
</Tabs>

## Props

### ModalSelectorProps

| Prop             | Type    | Default |
|------------------|---------|---------|
| showCapabilities | boolean | `true`  |
| showDescription  | boolean | `true`  |
